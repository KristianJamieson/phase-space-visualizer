import React from "react";
import ReactDOM from "react-dom/client";
import PhaseSpaceApplet from "./PhaseSpaceApplet";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <PhaseSpaceApplet />
  </React.StrictMode>
);
